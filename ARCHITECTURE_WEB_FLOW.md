# FACTOR HIBRID - 웹 아키텍처 및 데이터 흐름 문서

> 작성일: 2025-11-25
> 버전: 1.0
> 대상: 웹 플랫폼 (packages/web)

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [기술 스택](#2-기술-스택)
3. [모노레포 아키텍처](#3-모노레포-아키텍처)
4. [인증 플로우](#4-인증-플로우)
5. [프린터 관리 플로우](#5-프린터-관리-플로우)
6. [실시간 통신 (MQTT/WebSocket)](#6-실시간-통신-mqttwebsocket)
7. [AI 워크플로우](#7-ai-워크플로우)
8. [결제 시스템](#8-결제-시스템)
9. [상태 관리](#9-상태-관리)
10. [라우팅 구조](#10-라우팅-구조)
11. [컴포넌트 아키텍처](#11-컴포넌트-아키텍처)
12. [데이터베이스 스키마](#12-데이터베이스-스키마)

---

## 1. 프로젝트 개요

**FACTOR HIBRID**는 3D 프린터 원격 관리 및 제어 플랫폼입니다.

### 핵심 기능
- 🖨️ **프린터 관리**: 다중 프린터 대시보드, 실시간 상태 모니터링
- 🎮 **프린터 제어**: 축 이동, 온도 조절, G-code 업로드/실행
- 📹 **카메라 통합**: MJPEG 스트림 실시간 모니터링
- 🤖 **AI 통합**: 이미지/텍스트 기반 3D 모델 생성
- 💳 **구독/결제**: TossPayments 기반 구독 관리
- 🌐 **다국어 지원**: 한국어/영어

### 타겟 플랫폼
```
┌─────────────────────────────────────────────────────┐
│                    FACTOR HIBRID                     │
├─────────────────┬─────────────────┬─────────────────┤
│       Web       │      iOS        │     Android     │
│   (브라우저)     │   (Capacitor)   │   (Capacitor)   │
└─────────────────┴─────────────────┴─────────────────┘
```

---

## 2. 기술 스택

### 2.1 프론트엔드 코어

| 카테고리 | 기술 | 버전 | 용도 |
|---------|------|------|------|
| **프레임워크** | React | 18.3.1 | UI 라이브러리 |
| **언어** | TypeScript | 5.5.3 | 타입 안정성 |
| **빌드 도구** | Vite | 5.4.1 | 번들링, HMR |
| **라우팅** | React Router DOM | 6.26.2 | 클라이언트 라우팅 |

### 2.2 UI/스타일링

| 카테고리 | 기술 | 버전 | 용도 |
|---------|------|------|------|
| **CSS** | Tailwind CSS | 3.4.11 | 유틸리티 기반 스타일링 |
| **컴포넌트** | Radix UI | - | 접근성 보장 헤드리스 UI |
| **아이콘** | Lucide React | 0.462.0 | 아이콘 라이브러리 |
| **테마** | next-themes | 0.3.0 | 다크/라이트 모드 |
| **애니메이션** | tailwindcss-animate | 1.0.7 | CSS 애니메이션 |

### 2.3 상태 관리

| 카테고리 | 기술 | 버전 | 용도 |
|---------|------|------|------|
| **서버 상태** | TanStack React Query | 5.56.2 | 캐싱, 동기화 |
| **클라이언트 상태** | React Context | - | 인증, UI 상태 |
| **영속 상태** | localStorage | - | 세션, 설정 |

### 2.4 백엔드/데이터베이스

| 카테고리 | 기술 | 버전 | 용도 |
|---------|------|------|------|
| **BaaS** | Supabase | 2.51.0 | PostgreSQL + Auth |
| **API 서버** | Express.js | 4.18.2 | REST API |
| **실시간** | MQTT | - | 디바이스 통신 |
| **실시간** | WebSocket | - | 상태 브로드캐스트 |

### 2.5 3D 시각화

| 카테고리 | 기술 | 버전 | 용도 |
|---------|------|------|------|
| **3D 엔진** | Three.js | 0.180.0 | 3D 그래픽스 |
| **React 바인딩** | React Three Fiber | 8.18.0 | Three.js React 통합 |
| **유틸리티** | @react-three/drei | 9.122.0 | 헬퍼 컴포넌트 |

### 2.6 결제

| 카테고리 | 기술 | 버전 | 용도 |
|---------|------|------|------|
| **결제 SDK** | TossPayments | 2.4.0 | 카드/간편결제 |
| **위젯** | Payment Widget SDK | 0.12.0 | 결제 UI |

### 2.7 폼/검증

| 카테고리 | 기술 | 버전 | 용도 |
|---------|------|------|------|
| **폼** | react-hook-form | 7.53.0 | 폼 상태 관리 |
| **검증** | Zod | 3.23.8 | 런타임 타입 검증 |

### 2.8 국제화

| 카테고리 | 기술 | 버전 | 용도 |
|---------|------|------|------|
| **i18n** | i18next | 25.6.0 | 번역 프레임워크 |
| **React 바인딩** | react-i18next | 16.0.1 | React 통합 |

---

## 3. 모노레포 아키텍처

### 3.1 패키지 구조

```
FACTOR-HIBRID-r1.0/
│
├── packages/
│   │
│   ├── host/                    # 🚪 플랫폼 디스패처
│   │   ├── src/
│   │   │   ├── main.tsx         # 진입점
│   │   │   ├── pages/
│   │   │   │   └── Dashboard.tsx # 플랫폼 감지 & 라우팅
│   │   │   └── lib/
│   │   │       └── platform.ts  # 플랫폼 감지 로직
│   │   └── vite.config.ts
│   │
│   ├── web/                     # 🌐 웹 애플리케이션
│   │   ├── src/
│   │   │   ├── App.tsx          # 메인 앱 (라우팅)
│   │   │   ├── main.tsx         # 진입점
│   │   │   ├── pages/           # 20+ 페이지
│   │   │   ├── components/      # UI 컴포넌트
│   │   │   │   ├── ui/          # Radix UI 래퍼
│   │   │   │   └── ai/          # AI 관련
│   │   │   ├── hooks/           # 커스텀 훅
│   │   │   └── services/        # 플랫폼 서비스
│   │   └── vite.config.ts
│   │
│   ├── mobile/                  # 📱 모바일 앱 (Capacitor)
│   │   ├── src/
│   │   │   ├── App.tsx          # 모바일 앱
│   │   │   ├── pages/           # 모바일 최적화 페이지
│   │   │   └── components/      # 모바일 컴포넌트
│   │   ├── android/             # Android 네이티브
│   │   ├── ios/                 # iOS 네이티브
│   │   └── capacitor.config.ts
│   │
│   └── shared/                  # 🔗 공유 패키지 (가장 큼)
│       ├── src/
│       │   ├── api/             # API 클라이언트 (10개 파일)
│       │   ├── queries/         # React Query 훅 (6개 파일)
│       │   ├── services/        # 비즈니스 로직 (12개 파일)
│       │   │   ├── supabaseService/
│       │   │   └── mqttService/
│       │   ├── contexts/
│       │   │   └── AuthContext.tsx  # 인증 컨텍스트
│       │   ├── component/
│       │   │   ├── mqtt.ts      # MQTT 클라이언트
│       │   │   └── websocket.ts # WebSocket 클라이언트
│       │   ├── types/           # TypeScript 타입
│       │   ├── hooks/           # 공유 훅
│       │   ├── i18n/            # 국제화
│       │   │   └── locales/
│       │   │       ├── en.ts    # 영어
│       │   │       └── ko.ts    # 한국어
│       │   └── integrations/
│       │       └── supabase/
│       └── server.js            # Express 백엔드 서버
│
├── package.json                 # 워크스페이스 설정
└── tsconfig.base.json           # 기본 TypeScript 설정
```

### 3.2 패키지 간 의존성

```
┌─────────────────────────────────────────────────────────────┐
│                         host                                 │
│                    (플랫폼 디스패처)                          │
│                          │                                   │
│            ┌─────────────┴─────────────┐                    │
│            ▼                           ▼                    │
│    ┌───────────────┐           ┌───────────────┐           │
│    │      web      │           │    mobile     │           │
│    │   (브라우저)   │           │  (Capacitor)  │           │
│    └───────┬───────┘           └───────┬───────┘           │
│            │                           │                    │
│            └───────────┬───────────────┘                    │
│                        ▼                                    │
│               ┌───────────────┐                             │
│               │    shared     │                             │
│               │  (공유 코드)   │                             │
│               └───────────────┘                             │
│                        │                                    │
│    ┌───────────────────┼───────────────────┐               │
│    ▼                   ▼                   ▼               │
│ ┌──────┐         ┌──────────┐         ┌──────┐            │
│ │ MQTT │         │ Supabase │         │ API  │            │
│ │Broker│         │(DB/Auth) │         │Server│            │
│ └──────┘         └──────────┘         └──────┘            │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 임포트 패턴

```typescript
// ✅ 좋은 예: 공유 패키지 임포트
import { useAuth } from '@shared/contexts/AuthContext';
import { usePrinterSnapshot } from '@shared/queries/printer';
import type { PrinterState } from '@shared/types/printerType';

// ✅ 좋은 예: 로컬 임포트
import { Header } from '@/components/Header';
import { CameraFeed } from '@/components/CameraFeed';

// ❌ 나쁜 예: 패키지 간 직접 상대 경로 임포트
// import something from '../../../shared/src/...'
```

---

## 4. 인증 플로우

### 4.1 인증 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           인증 플로우                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────────┐     ┌─────────────┐                 │
│  │  사용자   │────▶│  Auth 페이지  │────▶│ AuthContext │                 │
│  └──────────┘     └──────┬───────┘     └──────┬──────┘                 │
│                          │                     │                        │
│         ┌────────────────┼────────────────┐   │                        │
│         ▼                ▼                ▼   │                        │
│  ┌────────────┐  ┌────────────┐  ┌──────────┐│                        │
│  │이메일/비번  │  │   Google   │  │  Apple   ││                        │
│  │   로그인   │  │   OAuth    │  │  OAuth   ││                        │
│  └─────┬──────┘  └─────┬──────┘  └────┬─────┘│                        │
│        │               │              │      │                        │
│        └───────────────┼──────────────┘      │                        │
│                        ▼                     │                        │
│              ┌─────────────────┐             │                        │
│              │  Supabase Auth  │◀────────────┘                        │
│              └────────┬────────┘                                      │
│                       │                                               │
│                       ▼                                               │
│              ┌─────────────────┐                                      │
│              │  세션 생성/검증  │                                      │
│              └────────┬────────┘                                      │
│                       │                                               │
│         ┌─────────────┼─────────────┐                                │
│         ▼             ▼             ▼                                │
│  ┌────────────┐ ┌──────────┐ ┌─────────────┐                        │
│  │   프로필   │ │   역할   │ │    MQTT     │                        │
│  │  설정 확인 │ │   조회   │ │  구독 시작  │                        │
│  └─────┬──────┘ └────┬─────┘ └──────┬──────┘                        │
│        │             │              │                                │
│        ▼             ▼              ▼                                │
│  ┌────────────┐ ┌──────────┐ ┌─────────────┐                        │
│  │ /profile-  │ │user_roles│ │실시간 상태   │                        │
│  │   setup    │ │  테이블  │ │  업데이트   │                        │
│  └────────────┘ └──────────┘ └─────────────┘                        │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### 4.2 인증 상태 흐름

```
┌─────────────────────────────────────────────────────────────────────┐
│                        인증 상태 머신                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   ┌──────────┐    로그인 시도    ┌───────────┐                     │
│   │  IDLE    │ ─────────────────▶│  LOADING  │                     │
│   │ (미인증)  │◀─────────────────│           │                     │
│   └──────────┘    실패/로그아웃   └─────┬─────┘                     │
│        │                               │                            │
│        │                               │ 성공                       │
│        │                               ▼                            │
│        │                        ┌───────────┐                      │
│        │                        │ 프로필체크 │                      │
│        │                        └─────┬─────┘                      │
│        │                              │                            │
│        │              ┌───────────────┼───────────────┐            │
│        │              ▼               ▼               ▼            │
│        │       ┌───────────┐  ┌───────────┐  ┌───────────┐        │
│        │       │ 프로필 미설정│  │  이메일   │  │  인증완료  │        │
│        │       │            │  │  미인증   │  │           │        │
│        │       └─────┬─────┘  └─────┬─────┘  └─────┬─────┘        │
│        │             │              │              │               │
│        │             ▼              ▼              ▼               │
│        │      ┌───────────┐ ┌───────────┐  ┌───────────┐          │
│        │      │/profile-  │ │  /email-  │  │ /dashboard│          │
│        │      │  setup    │ │verification│  │           │          │
│        │      └───────────┘ └───────────┘  └───────────┘          │
│        │                                                           │
└────────┴───────────────────────────────────────────────────────────┘
```

### 4.3 관련 파일

| 파일 | 역할 |
|------|------|
| `packages/shared/src/contexts/AuthContext.tsx` | 인증 컨텍스트, 세션 관리 |
| `packages/web/src/pages/Auth.tsx` | 로그인/회원가입 UI |
| `packages/web/src/pages/AuthCallback.tsx` | OAuth 콜백 처리 |
| `packages/web/src/pages/ProfileSetup.tsx` | 프로필 설정 |
| `packages/web/src/pages/EmailVerification.tsx` | 이메일 인증 |
| `packages/web/src/components/ProtectedRoute.tsx` | 인증 보호 라우트 |
| `packages/shared/src/integrations/supabase/client.ts` | Supabase 클라이언트 |

### 4.4 AuthContext 주요 기능

```typescript
interface AuthContextType {
  // 상태
  user: User | null;
  session: Session | null;
  isLoading: boolean;
  role: string | null;

  // 액션
  signIn: (email: string, password: string) => Promise<void>;
  signUp: (email: string, password: string) => Promise<void>;
  signOut: () => Promise<void>;
  signInWithGoogle: () => Promise<void>;
  signInWithApple: () => Promise<void>;

  // 상태 체크
  isAdmin: () => boolean;
  needsProfileSetup: () => boolean;
}
```

---

## 5. 프린터 관리 플로우

### 5.1 프린터 관리 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         프린터 관리 시스템                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        Dashboard 페이지                          │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │   │
│  │  │ PrinterCard │  │ PrinterCard │  │ PrinterCard │  ...         │   │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘              │   │
│  └─────────┼────────────────┼────────────────┼─────────────────────┘   │
│            │                │                │                          │
│            └────────────────┼────────────────┘                          │
│                             ▼                                           │
│              ┌─────────────────────────────┐                           │
│              │     usePrinterSnapshot()    │                           │
│              │     (React Query Hook)      │                           │
│              └──────────────┬──────────────┘                           │
│                             │                                           │
│         ┌───────────────────┼───────────────────┐                      │
│         ▼                   ▼                   ▼                      │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐                │
│  │   Supabase  │    │    MQTT     │    │  Express    │                │
│  │  (메타데이터)│    │ (실시간상태) │    │   (REST)    │                │
│  └──────┬──────┘    └──────┬──────┘    └──────┬──────┘                │
│         │                  │                  │                        │
│         ▼                  ▼                  ▼                        │
│  ┌─────────────────────────────────────────────────┐                  │
│  │                   프린터 데이터                   │                  │
│  │  • 이름, UUID, 그룹                              │                  │
│  │  • 연결 상태 (connected/disconnected)           │                  │
│  │  • 온도 (노즐, 베드)                            │                  │
│  │  • 위치 (X, Y, Z, E)                           │                  │
│  │  • 인쇄 진행률                                   │                  │
│  │  • 출력 상태 (printing/paused/idle)             │                  │
│  └─────────────────────────────────────────────────┘                  │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

### 5.2 프린터 제어 플로우

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          프린터 제어 플로우                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    Printer Detail 페이지                         │   │
│  │                                                                  │   │
│  │  ┌──────────────────────────────────────────────────────────┐   │   │
│  │  │                    PrinterControlPad                      │   │   │
│  │  │                                                           │   │   │
│  │  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐        │   │   │
│  │  │  │   Home  │ │  이동   │ │  온도   │ │ G-code  │        │   │   │
│  │  │  │   버튼  │ │  조이패드│ │  설정   │ │  업로드 │        │   │   │
│  │  │  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘        │   │   │
│  │  └───────┼───────────┼───────────┼───────────┼──────────────┘   │   │
│  └──────────┼───────────┼───────────┼───────────┼──────────────────┘   │
│             │           │           │           │                       │
│             └───────────┴─────┬─────┴───────────┘                       │
│                               ▼                                         │
│                    ┌─────────────────────┐                             │
│                    │   MQTT Publish      │                             │
│                    │ (control/{uuid})    │                             │
│                    └──────────┬──────────┘                             │
│                               │                                         │
│                               ▼                                         │
│                    ┌─────────────────────┐                             │
│                    │    MQTT Broker      │                             │
│                    └──────────┬──────────┘                             │
│                               │                                         │
│                               ▼                                         │
│                    ┌─────────────────────┐                             │
│                    │   3D 프린터 장치     │                             │
│                    │   (OctoPrint)       │                             │
│                    └──────────┬──────────┘                             │
│                               │                                         │
│                               ▼                                         │
│                    ┌─────────────────────┐                             │
│                    │  MQTT Subscribe     │                             │
│                    │(control_result/{uuid})│                           │
│                    └──────────┬──────────┘                             │
│                               │                                         │
│                               ▼                                         │
│                    ┌─────────────────────┐                             │
│                    │   UI 업데이트       │                             │
│                    │   (Toast/상태)      │                             │
│                    └─────────────────────┘                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.3 프린터 상태 구조

```typescript
interface PrinterSnapshot {
  // 연결 정보
  connection: {
    status: 'connected' | 'disconnected' | 'connecting';
    lastSeen: string;
  };

  // 온도 정보
  temperature: {
    tool0: { actual: number; target: number };  // 노즐
    bed: { actual: number; target: number };    // 베드
  };

  // 위치 정보
  position: {
    x: number;
    y: number;
    z: number;
    e: number;  // 익스트루더
  };

  // 인쇄 진행 정보
  progress: {
    completion: number;        // 0-100
    printTime: number;         // 초
    printTimeLeft: number;     // 초
    fileName: string;
  };

  // 프린터 상태
  state: {
    text: string;              // "Printing", "Operational", etc.
    flags: {
      printing: boolean;
      paused: boolean;
      operational: boolean;
      error: boolean;
    };
  };
}
```

### 5.4 관련 파일

| 파일 | 역할 |
|------|------|
| `packages/web/src/pages/Dashboard.tsx` | 대시보드 페이지 |
| `packages/web/src/pages/PrinterDetail.tsx` | 프린터 상세 페이지 |
| `packages/web/src/components/PrinterCard.tsx` | 프린터 카드 컴포넌트 |
| `packages/web/src/components/PrinterControlPad.tsx` | 제어 패드 |
| `packages/web/src/components/TemperaturePanel.tsx` | 온도 패널 |
| `packages/web/src/components/PrintProgress.tsx` | 진행률 표시 |
| `packages/shared/src/queries/printer.ts` | React Query 훅 |
| `packages/shared/src/api/printer.ts` | API 클라이언트 |
| `packages/shared/src/services/supabaseService/printerList.ts` | Supabase 서비스 |

---

## 6. 실시간 통신 (MQTT/WebSocket)

### 6.1 실시간 통신 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        실시간 통신 아키텍처                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                         웹 클라이언트                              │  │
│  │                                                                   │  │
│  │    ┌─────────────────┐         ┌─────────────────┐              │  │
│  │    │   MQTT Client   │         │ WebSocket Client│              │  │
│  │    │  (mqtt.js)      │         │                 │              │  │
│  │    └────────┬────────┘         └────────┬────────┘              │  │
│  └─────────────┼───────────────────────────┼────────────────────────┘  │
│                │                           │                            │
│                │ WebSocket                 │ WebSocket                  │
│                │ (ws://broker:port)        │ (ws://server:port)         │
│                ▼                           ▼                            │
│  ┌─────────────────────┐       ┌─────────────────────┐                │
│  │    MQTT Broker      │       │   Express Server    │                │
│  │    (Mosquitto)      │       │   (server.js)       │                │
│  └──────────┬──────────┘       └──────────┬──────────┘                │
│             │                              │                            │
│             │ MQTT Protocol                │ HTTP/WebSocket             │
│             ▼                              ▼                            │
│  ┌─────────────────────┐       ┌─────────────────────┐                │
│  │   3D Printer        │       │     Supabase        │                │
│  │   (OctoPrint)       │       │  (메타데이터 저장)   │                │
│  └─────────────────────┘       └─────────────────────┘                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 6.2 MQTT 토픽 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         MQTT 토픽 구조                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  📥 구독 토픽 (Subscribe)                                               │
│  ─────────────────────────────────────────────────────────────────      │
│  │                                                                      │
│  │  octoprint/status/{device_uuid}                                     │
│  │  └─▶ 프린터 상태 업데이트 (온도, 위치, 진행률, 연결상태)              │
│  │                                                                      │
│  │  control_result/{device_uuid}                                       │
│  │  └─▶ 제어 명령 결과 (성공/실패)                                     │
│  │                                                                      │
│  │  ai/model_completed                                                 │
│  │  └─▶ AI 모델 생성 완료 알림                                         │
│  │                                                                      │
│  │  ai/model_failed                                                    │
│  │  └─▶ AI 모델 생성 실패 알림                                         │
│  │                                                                      │
│                                                                         │
│  📤 발행 토픽 (Publish)                                                 │
│  ─────────────────────────────────────────────────────────────────      │
│  │                                                                      │
│  │  control/{device_uuid}                                              │
│  │  └─▶ 프린터 제어 명령 (이동, 온도설정, 인쇄시작/중지)                │
│  │                                                                      │
│  │  gcode/{device_uuid}                                                │
│  │  └─▶ G-code 파일 전송                                               │
│  │                                                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 6.3 MQTT 메시지 포맷

```typescript
// 상태 메시지 (octoprint/status/{uuid})
interface StatusMessage {
  type: 'status';
  timestamp: string;
  data: {
    connection: ConnectionStatus;
    temperature: TemperatureData;
    position: PositionData;
    progress: ProgressData;
    state: PrinterState;
  };
}

// 제어 명령 (control/{uuid})
interface ControlCommand {
  type: 'home' | 'move' | 'temperature' | 'print' | 'pause' | 'cancel';
  payload: {
    axis?: 'x' | 'y' | 'z' | 'all';
    x?: number;
    y?: number;
    z?: number;
    tool?: number;
    bed?: number;
    filename?: string;
  };
}

// 제어 결과 (control_result/{uuid})
interface ControlResult {
  success: boolean;
  command: string;
  message?: string;
  error?: string;
}
```

### 6.4 MQTT 연결 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       MQTT 연결 라이프사이클                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌──────────┐                                                         │
│   │   로그인  │                                                         │
│   └────┬─────┘                                                         │
│        │                                                               │
│        ▼                                                               │
│   ┌─────────────────────────────────────────────┐                     │
│   │            AuthContext.tsx                   │                     │
│   │                                              │                     │
│   │  useEffect(() => {                          │                     │
│   │    if (user && !mqttInitialized) {          │                     │
│   │      initializeMqttSubscription();          │                     │
│   │    }                                         │                     │
│   │  }, [user]);                                │                     │
│   │                                              │                     │
│   └───────────────────┬─────────────────────────┘                     │
│                       │                                                │
│                       ▼                                                │
│   ┌─────────────────────────────────────────────┐                     │
│   │           mqttService/index.ts               │                     │
│   │                                              │                     │
│   │  • 싱글톤 클라이언트 생성                     │                     │
│   │  • WebSocket 브릿지 연결                     │                     │
│   │  • 프린터별 토픽 구독                        │                     │
│   │                                              │                     │
│   └───────────────────┬─────────────────────────┘                     │
│                       │                                                │
│        ┌──────────────┴──────────────┐                                │
│        ▼                             ▼                                │
│   ┌─────────────┐             ┌─────────────┐                        │
│   │  구독 성공  │             │  구독 실패  │                        │
│   └──────┬──────┘             └──────┬──────┘                        │
│          │                           │                                │
│          ▼                           ▼                                │
│   ┌─────────────┐             ┌─────────────┐                        │
│   │ 실시간 상태 │             │  재연결     │                        │
│   │   업데이트  │             │  시도       │                        │
│   └─────────────┘             └─────────────┘                        │
│                                                                       │
│   ┌──────────┐                                                       │
│   │  로그아웃 │ ──▶ MQTT 연결 해제 & 구독 해제                       │
│   └──────────┘                                                       │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

### 6.5 관련 파일

| 파일 | 역할 |
|------|------|
| `packages/shared/src/component/mqtt.ts` | MQTT 클라이언트 (500+ 라인) |
| `packages/shared/src/services/mqttService/index.ts` | MQTT 서비스 파사드 |
| `packages/shared/src/component/websocket.ts` | WebSocket 클라이언트 |
| `packages/shared/src/contexts/AuthContext.tsx` | MQTT 구독 관리 |
| `packages/shared/server.js` | WebSocket 서버 |

---

## 7. AI 워크플로우

### 7.1 AI 모델 생성 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         AI 워크플로우                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                      Create 페이지                               │   │
│  │                                                                  │   │
│  │   ┌───────────────────┐    ┌───────────────────┐               │   │
│  │   │    이미지 업로드    │    │    텍스트 입력    │               │   │
│  │   │   (Image-to-3D)   │    │   (Text-to-3D)   │               │   │
│  │   └─────────┬─────────┘    └─────────┬─────────┘               │   │
│  └─────────────┼─────────────────────────┼──────────────────────────┘   │
│                │                         │                              │
│                └───────────┬─────────────┘                              │
│                            ▼                                            │
│              ┌─────────────────────────────┐                           │
│              │      AI Service API         │                           │
│              │    (aiWorkflow.ts)          │                           │
│              └──────────────┬──────────────┘                           │
│                             │                                           │
│                             ▼                                           │
│              ┌─────────────────────────────┐                           │
│              │     Supabase Storage        │                           │
│              │    (이미지/모델 저장)        │                           │
│              └──────────────┬──────────────┘                           │
│                             │                                           │
│                             ▼                                           │
│              ┌─────────────────────────────┐                           │
│              │     AI Processing Server    │                           │
│              │   (외부 AI 모델 서버)        │                           │
│              └──────────────┬──────────────┘                           │
│                             │                                           │
│         ┌───────────────────┼───────────────────┐                      │
│         ▼                   ▼                   ▼                      │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐                │
│  │   처리중    │    │   완료      │    │   실패      │                │
│  │  (pending)  │    │ (completed) │    │  (failed)   │                │
│  └──────┬──────┘    └──────┬──────┘    └──────┬──────┘                │
│         │                  │                  │                        │
│         │                  ▼                  │                        │
│         │           ┌─────────────┐           │                        │
│         │           │  MQTT 알림  │◀──────────┘                        │
│         │           │ (ai/model_*)│                                    │
│         │           └──────┬──────┘                                    │
│         │                  │                                           │
│         ▼                  ▼                                           │
│  ┌─────────────────────────────────┐                                  │
│  │       AI 모델 목록 업데이트      │                                  │
│  │    (Dashboard / AI Archive)    │                                  │
│  └─────────────────────────────────┘                                  │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

### 7.2 AI 모델 상태 머신

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       AI 모델 상태 흐름                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌──────────┐     업로드      ┌───────────┐                          │
│   │   없음   │ ───────────────▶│  pending  │                          │
│   │          │                 │  (대기중)  │                          │
│   └──────────┘                 └─────┬─────┘                          │
│                                      │                                 │
│                    ┌─────────────────┼─────────────────┐              │
│                    │                 │                 │              │
│                    ▼                 ▼                 ▼              │
│             ┌───────────┐    ┌───────────┐    ┌───────────┐          │
│             │processing │    │ completed │    │  failed   │          │
│             │ (처리중)  │───▶│  (완료)   │    │  (실패)   │          │
│             └───────────┘    └───────────┘    └─────┬─────┘          │
│                                    │                 │                │
│                                    ▼                 ▼                │
│                             ┌───────────┐    ┌───────────┐           │
│                             │  다운로드  │    │  재시도   │           │
│                             │   가능    │    │   가능    │           │
│                             └───────────┘    └───────────┘           │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

### 7.3 AI 모델 데이터 구조

```typescript
interface AIModel {
  id: string;
  user_id: string;

  // 입력 정보
  input_type: 'image' | 'text';
  input_image_url?: string;
  input_text?: string;

  // 출력 정보
  output_model_url?: string;      // 3D 모델 파일 URL
  thumbnail_url?: string;         // 썸네일 이미지

  // 상태
  status: 'pending' | 'processing' | 'completed' | 'failed';
  error_message?: string;

  // 메타데이터
  name: string;
  description?: string;
  tags?: string[];

  // 타임스탬프
  created_at: string;
  updated_at: string;
  completed_at?: string;
}
```

### 7.4 3D 모델 뷰어 컴포넌트

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       ModelViewer 컴포넌트                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                     ModelViewer.tsx                              │   │
│  │                                                                  │   │
│  │   ┌─────────────────────────────────────────────────────────┐   │   │
│  │   │                  React Three Fiber                       │   │   │
│  │   │                    <Canvas>                              │   │   │
│  │   │                                                          │   │   │
│  │   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │   │   │
│  │   │   │  Lighting   │  │   Camera    │  │   Controls  │    │   │   │
│  │   │   │ (조명설정)  │  │ (카메라)    │  │(OrbitControls)│  │   │   │
│  │   │   └─────────────┘  └─────────────┘  └─────────────┘    │   │   │
│  │   │                                                          │   │   │
│  │   │   ┌───────────────────────────────────────────────┐     │   │   │
│  │   │   │              STL/OBJ Loader                    │     │   │   │
│  │   │   │         (3D 모델 로드 및 렌더링)                │     │   │   │
│  │   │   └───────────────────────────────────────────────┘     │   │   │
│  │   │                                                          │   │   │
│  │   └──────────────────────────────────────────────────────────┘   │   │
│  │                                                                  │   │
│  │   기능:                                                          │   │
│  │   • STL/OBJ 파일 로드                                           │   │
│  │   • 마우스 드래그로 회전                                         │   │
│  │   • 스크롤로 줌 인/아웃                                          │   │
│  │   • 터치 제스처 지원                                             │   │
│  │   • 와이어프레임/솔리드 토글                                      │   │
│  │   • 그리드 표시                                                  │   │
│  │                                                                  │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 7.5 관련 파일

| 파일 | 역할 |
|------|------|
| `packages/web/src/pages/Create.tsx` | AI 생성 페이지 |
| `packages/web/src/components/ModelViewer.tsx` | 3D 모델 뷰어 (36KB) |
| `packages/web/src/components/ai/` | AI 관련 컴포넌트 |
| `packages/shared/src/api/aiWorkflow.ts` | AI API 클라이언트 |
| `packages/shared/src/queries/aiModel.ts` | React Query 훅 |
| `packages/shared/src/services/aiModel.ts` | AI 모델 서비스 |
| `packages/shared/src/services/aiStorage.ts` | 모델 스토리지 서비스 |
| `packages/shared/src/types/aiModelType.ts` | AI 모델 타입 정의 |

---

## 8. 결제 시스템

### 8.1 결제 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          결제 시스템                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    Subscription 페이지                           │   │
│  │                                                                  │   │
│  │   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │   │
│  │   │  Free Plan   │  │ Basic Plan   │  │  Pro Plan    │         │   │
│  │   │    (무료)     │  │   (기본)     │  │   (프로)     │         │   │
│  │   └──────┬───────┘  └──────┬───────┘  └──────┬───────┘         │   │
│  └──────────┼─────────────────┼─────────────────┼──────────────────┘   │
│             │                 │                 │                       │
│             └─────────────────┼─────────────────┘                       │
│                               ▼                                         │
│                    ┌─────────────────────┐                             │
│                    │  결제 버튼 클릭     │                             │
│                    └──────────┬──────────┘                             │
│                               │                                         │
│                               ▼                                         │
│                    ┌─────────────────────┐                             │
│                    │    PaymentDialog    │                             │
│                    │   (결제 다이얼로그)  │                             │
│                    └──────────┬──────────┘                             │
│                               │                                         │
│                               ▼                                         │
│              ┌─────────────────────────────────┐                       │
│              │      TossPayments Widget        │                       │
│              │                                 │                       │
│              │  ┌────────┐  ┌────────┐        │                       │
│              │  │카드결제 │  │간편결제 │ ...   │                       │
│              │  └────────┘  └────────┘        │                       │
│              └───────────────┬─────────────────┘                       │
│                              │                                          │
│               ┌──────────────┼──────────────┐                          │
│               ▼              ▼              ▼                          │
│        ┌───────────┐  ┌───────────┐  ┌───────────┐                    │
│        │  결제성공  │  │  결제실패  │  │  취소     │                    │
│        └─────┬─────┘  └─────┬─────┘  └─────┬─────┘                    │
│              │              │              │                           │
│              ▼              ▼              ▼                           │
│        ┌───────────┐  ┌───────────┐  ┌───────────┐                    │
│        │/payment/  │  │/payment/  │  │ 이전페이지│                    │
│        │ success   │  │  fail     │  │   복귀   │                    │
│        └─────┬─────┘  └───────────┘  └───────────┘                    │
│              │                                                         │
│              ▼                                                         │
│       ┌─────────────────────────────────┐                             │
│       │       Supabase 구독 업데이트     │                             │
│       │    (subscriptions 테이블)       │                             │
│       └─────────────────────────────────┘                             │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

### 8.2 구독 플랜 구조

```typescript
interface SubscriptionPlan {
  id: string;
  name: 'free' | 'basic' | 'pro' | 'enterprise';

  // 가격 정보
  price: {
    monthly: number;
    yearly: number;
  };

  // 기능 제한
  limits: {
    printers: number;           // 등록 가능 프린터 수
    aiModels: number;           // 월간 AI 모델 생성 수
    storage: number;            // 저장 용량 (GB)
    teamMembers?: number;       // 팀 멤버 수
  };

  // 기능 플래그
  features: {
    realtimeMonitoring: boolean;
    remoteControl: boolean;
    aiGeneration: boolean;
    prioritySupport: boolean;
    apiAccess: boolean;
  };
}

// 사용자 구독 정보
interface UserSubscription {
  id: string;
  user_id: string;
  plan_id: string;

  // 상태
  status: 'active' | 'canceled' | 'past_due' | 'trialing';

  // 기간
  current_period_start: string;
  current_period_end: string;
  cancel_at_period_end: boolean;

  // 결제 정보
  payment_method_id?: string;

  // 타임스탬프
  created_at: string;
  updated_at: string;
}
```

### 8.3 TossPayments 통합

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      TossPayments 결제 흐름                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. 위젯 초기화                                                         │
│  ─────────────────────────────────────────────────────────────────      │
│     tossPayments.widgets({                                             │
│       clientKey: TOSS_CLIENT_KEY,                                      │
│       customerKey: user.id                                             │
│     })                                                                 │
│                                                                         │
│  2. 결제 수단 렌더링                                                    │
│  ─────────────────────────────────────────────────────────────────      │
│     widgets.renderPaymentMethods({                                     │
│       selector: '#payment-widget',                                     │
│       variantKey: 'default'                                            │
│     })                                                                 │
│                                                                         │
│  3. 결제 요청                                                           │
│  ─────────────────────────────────────────────────────────────────      │
│     widgets.requestPayment({                                           │
│       orderId: generateOrderId(),                                      │
│       orderName: 'FACTOR Pro 구독',                                    │
│       successUrl: '/payment/success',                                  │
│       failUrl: '/payment/fail',                                        │
│       customerEmail: user.email,                                       │
│       customerName: user.name                                          │
│     })                                                                 │
│                                                                         │
│  4. 콜백 처리                                                           │
│  ─────────────────────────────────────────────────────────────────      │
│     /payment/success?paymentKey=xxx&orderId=xxx&amount=xxx             │
│     → 서버 검증 → 구독 활성화                                           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 8.4 관련 파일

| 파일 | 역할 |
|------|------|
| `packages/web/src/pages/Subscription.tsx` | 구독 관리 페이지 |
| `packages/web/src/pages/PaymentCheckout.tsx` | 결제 체크아웃 |
| `packages/web/src/pages/PaymentSuccess.tsx` | 결제 성공 |
| `packages/web/src/pages/PaymentFail.tsx` | 결제 실패 |
| `packages/web/src/components/PaymentDialog.tsx` | 결제 다이얼로그 |
| `packages/web/src/components/UpgradePrompt.tsx` | 업그레이드 프롬프트 |
| `packages/shared/src/services/tossPaymentsService.ts` | TossPayments 서비스 |
| `packages/shared/src/services/supabaseService/subscription.ts` | 구독 서비스 |
| `packages/shared/src/services/supabaseService/paymentMethod.ts` | 결제수단 서비스 |

---

## 9. 상태 관리

### 9.1 상태 관리 레이어

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         상태 관리 아키텍처                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                    서버 상태 (React Query)                       │  │
│   │                                                                  │  │
│   │   • 프린터 데이터 (usePrinterSnapshot, usePrinterStatus)        │  │
│   │   • 사용자 프로필 (useProfile)                                   │  │
│   │   • AI 모델 (useAIModels)                                       │  │
│   │   • 구독 정보 (useSubscription)                                  │  │
│   │                                                                  │  │
│   │   특징:                                                          │  │
│   │   • 자동 캐싱 & 재검증                                           │  │
│   │   • 백그라운드 리페칭                                            │  │
│   │   • 중복 요청 방지                                               │  │
│   │   • 낙관적 업데이트                                              │  │
│   │                                                                  │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                  클라이언트 상태 (React Context)                  │  │
│   │                                                                  │  │
│   │   ┌─────────────────┐  ┌─────────────────┐                      │  │
│   │   │   AuthContext   │  │ AISidebarContext│                      │  │
│   │   │                 │  │                 │                      │  │
│   │   │ • user         │  │ • isOpen        │                      │  │
│   │   │ • session      │  │ • width         │                      │  │
│   │   │ • role         │  │ • toggle()      │                      │  │
│   │   │ • isLoading    │  │                 │                      │  │
│   │   └─────────────────┘  └─────────────────┘                      │  │
│   │                                                                  │  │
│   │   ┌─────────────────┐  ┌─────────────────┐                      │  │
│   │   │  ThemeContext   │  │   I18nContext   │                      │  │
│   │   │ (next-themes)   │  │  (i18next)      │                      │  │
│   │   │                 │  │                 │                      │  │
│   │   │ • theme        │  │ • language      │                      │  │
│   │   │ • setTheme()   │  │ • t()           │                      │  │
│   │   └─────────────────┘  └─────────────────┘                      │  │
│   │                                                                  │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                    영속 상태 (localStorage)                      │  │
│   │                                                                  │  │
│   │   • 세션 토큰 (supabase.auth.token)                             │  │
│   │   • 테마 설정 (theme)                                           │  │
│   │   • 언어 설정 (i18nextLng)                                      │  │
│   │   • 플랫폼 오버라이드 (platformOverride)                         │  │
│   │   • MQTT 클라이언트 ID                                          │  │
│   │   • 사이드바 설정 (width, collapsed)                            │  │
│   │                                                                  │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                    실시간 상태 (MQTT/WebSocket)                   │  │
│   │                                                                  │  │
│   │   • 프린터 연결 상태                                             │  │
│   │   • 온도/위치/진행률 실시간 업데이트                              │  │
│   │   • AI 모델 완료/실패 이벤트                                     │  │
│   │   • 제어 명령 결과                                               │  │
│   │                                                                  │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 9.2 React Query 설정

```typescript
// QueryClient 설정
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      // 캐시 유지 시간: 5분
      staleTime: 5 * 60 * 1000,

      // 가비지 컬렉션 시간: 30분
      gcTime: 30 * 60 * 1000,

      // 백그라운드 리페칭 활성화
      refetchOnWindowFocus: true,

      // 실패 시 3번 재시도
      retry: 3,

      // 재시도 간격: 지수 백오프
      retryDelay: (attemptIndex) =>
        Math.min(1000 * 2 ** attemptIndex, 30000),
    },
  },
});

// 주요 쿼리 키
const queryKeys = {
  printers: ['printers'] as const,
  printer: (id: string) => ['printer', id] as const,
  printerSnapshot: (id: string) => ['printer', id, 'snapshot'] as const,
  aiModels: ['ai-models'] as const,
  subscription: ['subscription'] as const,
  profile: ['profile'] as const,
};
```

---

## 10. 라우팅 구조

### 10.1 라우트 맵

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           웹 라우팅 구조                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  📁 공개 라우트 (Public Routes)                                         │
│  ─────────────────────────────────────────────────────────────────      │
│  │                                                                      │
│  │  /                    → Home              랜딩 페이지               │
│  │  /auth                → Auth              로그인/회원가입           │
│  │  /auth/callback       → AuthCallback      OAuth 콜백               │
│  │  /supported-printers  → SupportedPrinters 지원 프린터 목록          │
│  │  /privacy             → PrivacyPolicy     개인정보처리방침          │
│  │  /terms               → Terms             이용약관                  │
│  │  /refund              → RefundPolicy      환불정책                  │
│  │  /setup/:uuid         → DeviceSetup       디바이스 설정             │
│  │                                                                      │
│                                                                         │
│  🔒 보호된 라우트 (Protected Routes)                                    │
│  ─────────────────────────────────────────────────────────────────      │
│  │                                                                      │
│  │  /dashboard           → Dashboard         메인 대시보드             │
│  │  /printer/:id         → PrinterDetail     프린터 상세               │
│  │  /create              → Create            AI 모델 생성              │
│  │  /settings            → Settings          앱 설정                   │
│  │  /user-settings       → UserSettings      사용자 설정               │
│  │  /subscription        → Subscription      구독 관리                 │
│  │  /profile-setup       → ProfileSetup      프로필 설정               │
│  │  /email-verification  → EmailVerification 이메일 인증               │
│  │                                                                      │
│                                                                         │
│  💳 결제 라우트 (Payment Routes)                                        │
│  ─────────────────────────────────────────────────────────────────      │
│  │                                                                      │
│  │  /payment/checkout    → PaymentCheckout   결제 위젯                 │
│  │  /payment/success     → PaymentSuccess    결제 성공                 │
│  │  /payment/fail        → PaymentFail       결제 실패                 │
│  │                                                                      │
│                                                                         │
│  👑 관리자 라우트 (Admin Routes)                                        │
│  ─────────────────────────────────────────────────────────────────      │
│  │                                                                      │
│  │  /admin               → Admin             관리자 패널               │
│  │  /admin/device/register → DeviceRegister  디바이스 등록             │
│  │                                                                      │
│                                                                         │
│  ❌ 에러 라우트                                                         │
│  ─────────────────────────────────────────────────────────────────      │
│  │                                                                      │
│  │  *                    → NotFound          404 페이지                │
│  │                                                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 10.2 라우트 보호 컴포넌트

```typescript
// ProtectedRoute - 인증 필요
const ProtectedRoute = ({ children }) => {
  const { user, isLoading } = useAuth();

  if (isLoading) return <LoadingSpinner />;
  if (!user) return <Navigate to="/auth" />;

  return children;
};

// AdminRoute - 관리자 권한 필요
const AdminRoute = ({ children }) => {
  const { user, isAdmin, isLoading } = useAuth();

  if (isLoading) return <LoadingSpinner />;
  if (!user) return <Navigate to="/auth" />;
  if (!isAdmin()) return <Navigate to="/dashboard" />;

  return children;
};
```

### 10.3 Lazy Loading

```typescript
// 페이지 Lazy Loading
const Dashboard = lazy(() => import('./pages/Dashboard'));
const PrinterDetail = lazy(() => import('./pages/PrinterDetail'));
const Create = lazy(() => import('./pages/Create'));
const Settings = lazy(() => import('./pages/Settings'));

// Suspense 래퍼
<Suspense fallback={<PageLoader />}>
  <Routes>
    <Route path="/dashboard" element={<Dashboard />} />
    ...
  </Routes>
</Suspense>
```

---

## 11. 컴포넌트 아키텍처

### 11.1 컴포넌트 계층 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        컴포넌트 계층 구조                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  📦 Layout Components (레이아웃)                                        │
│  ─────────────────────────────────────────────────────────────────      │
│  │                                                                      │
│  │  App.tsx                                                            │
│  │   ├─ QueryClientProvider                                            │
│  │   ├─ ThemeProvider                                                  │
│  │   ├─ I18nProvider                                                   │
│  │   ├─ AuthProvider                                                   │
│  │   ├─ ToastProvider (Sonner)                                         │
│  │   └─ BrowserRouter                                                  │
│  │       ├─ Header                                                     │
│  │       ├─ <Routes>                                                   │
│  │       │   └─ Page Components                                        │
│  │       ├─ Footer                                                     │
│  │       └─ AISidebar (웹 전용)                                        │
│  │                                                                      │
│                                                                         │
│  📄 Page Components (페이지)                                            │
│  ─────────────────────────────────────────────────────────────────      │
│  │                                                                      │
│  │  pages/                                                             │
│  │   ├─ Dashboard.tsx        (대시보드 - 프린터 목록)                  │
│  │   ├─ PrinterDetail.tsx    (프린터 상세)                             │
│  │   ├─ Create.tsx           (AI 생성)                                 │
│  │   ├─ Auth.tsx             (인증)                                    │
│  │   ├─ Settings.tsx         (설정)                                    │
│  │   ├─ Subscription.tsx     (구독)                                    │
│  │   └─ ...                                                            │
│  │                                                                      │
│                                                                         │
│  🧩 Feature Components (기능)                                           │
│  ─────────────────────────────────────────────────────────────────      │
│  │                                                                      │
│  │  components/                                                        │
│  │   ├─ PrinterCard.tsx          프린터 카드                           │
│  │   ├─ PrinterControlPad.tsx    제어 패드                             │
│  │   ├─ PrinterStatusBadge.tsx   상태 뱃지                             │
│  │   ├─ CameraFeed.tsx           카메라 피드                           │
│  │   ├─ TemperaturePanel.tsx     온도 패널                             │
│  │   ├─ PrintProgress.tsx        진행률                                │
│  │   ├─ ModelViewer.tsx          3D 뷰어                               │
│  │   ├─ GCodeUpload.tsx          G-code 업로드                         │
│  │   ├─ STLUpload.tsx            STL 업로드                            │
│  │   ├─ PaymentDialog.tsx        결제 다이얼로그                        │
│  │   └─ ...                                                            │
│  │                                                                      │
│                                                                         │
│  🎨 UI Components (Radix UI 기반)                                       │
│  ─────────────────────────────────────────────────────────────────      │
│  │                                                                      │
│  │  components/ui/                                                     │
│  │   ├─ button.tsx               버튼                                  │
│  │   ├─ dialog.tsx               다이얼로그                            │
│  │   ├─ dropdown-menu.tsx        드롭다운 메뉴                         │
│  │   ├─ select.tsx               셀렉트                                │
│  │   ├─ tabs.tsx                 탭                                    │
│  │   ├─ toast.tsx                토스트                                │
│  │   ├─ card.tsx                 카드                                  │
│  │   ├─ input.tsx                인풋                                  │
│  │   ├─ form.tsx                 폼                                    │
│  │   └─ ...                                                            │
│  │                                                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 11.2 주요 컴포넌트 관계도

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Dashboard 페이지 컴포넌트 구조                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Dashboard.tsx                                                         │
│   │                                                                    │
│   ├─ PrinterGroupList                                                  │
│   │   └─ PrinterGroup                                                  │
│   │       └─ PrinterCard (map)                                         │
│   │           ├─ PrinterStatusBadge                                    │
│   │           ├─ TemperatureDisplay                                    │
│   │           ├─ PrintProgress                                         │
│   │           └─ CameraPreview                                         │
│   │                                                                    │
│   ├─ DashboardSummary                                                  │
│   │   ├─ TotalPrinters                                                 │
│   │   ├─ ActivePrints                                                  │
│   │   └─ ErrorCount                                                    │
│   │                                                                    │
│   └─ AddPrinterDialog                                                  │
│       └─ PrinterSetupWizard                                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                 PrinterDetail 페이지 컴포넌트 구조                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  PrinterDetail.tsx                                                     │
│   │                                                                    │
│   ├─ PrinterHeader                                                     │
│   │   ├─ PrinterName                                                   │
│   │   ├─ PrinterStatusBadge                                            │
│   │   └─ PrinterActions                                                │
│   │                                                                    │
│   ├─ CameraFeed                                                        │
│   │   └─ MJPEGStream                                                   │
│   │                                                                    │
│   ├─ PrinterControlPad                                                 │
│   │   ├─ AxisControl (X, Y, Z)                                         │
│   │   ├─ HomeButtons                                                   │
│   │   ├─ ExtruderControl                                               │
│   │   └─ PrintActions                                                  │
│   │                                                                    │
│   ├─ TemperaturePanel                                                  │
│   │   ├─ NozzleTemp                                                    │
│   │   ├─ BedTemp                                                       │
│   │   └─ TargetControls                                                │
│   │                                                                    │
│   ├─ PrintProgress                                                     │
│   │   ├─ ProgressBar                                                   │
│   │   ├─ TimeRemaining                                                 │
│   │   └─ FileName                                                      │
│   │                                                                    │
│   └─ GCodeUpload                                                       │
│       ├─ FileDropzone                                                  │
│       ├─ GCodePreview                                                  │
│       └─ UploadProgress                                                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 12. 데이터베이스 스키마

### 12.1 Supabase 테이블 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       Supabase 테이블 스키마                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐              │
│  │   profiles  │     │   printers  │     │   cameras   │              │
│  ├─────────────┤     ├─────────────┤     ├─────────────┤              │
│  │ id (FK)     │◀────│ user_id     │     │ id          │              │
│  │ display_name│     │ id          │────▶│ printer_id  │              │
│  │ phone       │     │ name        │     │ stream_url  │              │
│  │ avatar_url  │     │ uuid        │     │ enabled     │              │
│  │ created_at  │     │ group_id    │     │ created_at  │              │
│  │ updated_at  │     │ created_at  │     └─────────────┘              │
│  └─────────────┘     └──────┬──────┘                                  │
│         │                   │                                          │
│         │                   │                                          │
│  ┌──────▼──────┐     ┌──────▼──────┐                                  │
│  │ user_roles  │     │printer_groups│                                  │
│  ├─────────────┤     ├─────────────┤                                  │
│  │ id          │     │ id          │                                  │
│  │ user_id     │     │ user_id     │                                  │
│  │ role        │     │ name        │                                  │
│  │ created_at  │     │ order       │                                  │
│  └─────────────┘     │ created_at  │                                  │
│                      └─────────────┘                                  │
│                                                                        │
│  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐              │
│  │  ai_models  │     │subscriptions│     │payment_methods│            │
│  ├─────────────┤     ├─────────────┤     ├─────────────┤              │
│  │ id          │     │ id          │     │ id          │              │
│  │ user_id     │     │ user_id     │     │ user_id     │              │
│  │ name        │     │ plan_id     │     │ type        │              │
│  │ input_type  │     │ status      │     │ card_last4  │              │
│  │ input_url   │     │ period_start│     │ is_default  │              │
│  │ output_url  │     │ period_end  │     │ created_at  │              │
│  │ status      │     │ created_at  │     └─────────────┘              │
│  │ created_at  │     └─────────────┘                                  │
│  └─────────────┘                                                       │
│                                                                        │
│  ┌─────────────┐     ┌─────────────┐                                  │
│  │  feedback   │     │notifications│                                  │
│  ├─────────────┤     ├─────────────┤                                  │
│  │ id          │     │ id          │                                  │
│  │ user_id     │     │ user_id     │                                  │
│  │ type        │     │ title       │                                  │
│  │ content     │     │ body        │                                  │
│  │ rating      │     │ read        │                                  │
│  │ created_at  │     │ created_at  │                                  │
│  └─────────────┘     └─────────────┘                                  │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

### 12.2 주요 테이블 상세

```typescript
// profiles 테이블
interface Profile {
  id: string;              // auth.users.id (FK)
  display_name: string | null;
  phone: string | null;
  avatar_url: string | null;
  created_at: string;
  updated_at: string;
}

// printers 테이블
interface Printer {
  id: string;              // UUID
  user_id: string;         // auth.users.id (FK)
  name: string;
  uuid: string;            // 디바이스 고유 ID
  group_id: string | null; // printer_groups.id (FK)
  model: string | null;    // 프린터 모델명
  firmware: string | null; // 펌웨어 버전
  created_at: string;
  updated_at: string;
}

// user_roles 테이블
interface UserRole {
  id: string;
  user_id: string;         // auth.users.id (FK)
  role: 'user' | 'admin' | 'superadmin';
  created_at: string;
}

// ai_models 테이블
interface AIModel {
  id: string;
  user_id: string;
  name: string;
  input_type: 'image' | 'text';
  input_image_url: string | null;
  input_text: string | null;
  output_model_url: string | null;
  thumbnail_url: string | null;
  status: 'pending' | 'processing' | 'completed' | 'failed';
  error_message: string | null;
  created_at: string;
  updated_at: string;
  completed_at: string | null;
}

// subscriptions 테이블
interface Subscription {
  id: string;
  user_id: string;
  plan_id: string;
  status: 'active' | 'canceled' | 'past_due' | 'trialing';
  current_period_start: string;
  current_period_end: string;
  cancel_at_period_end: boolean;
  payment_method_id: string | null;
  created_at: string;
  updated_at: string;
}
```

### 12.3 Row Level Security (RLS) 정책

```sql
-- profiles: 본인 데이터만 접근
CREATE POLICY "Users can view own profile" ON profiles
  FOR SELECT USING (auth.uid() = id);

CREATE POLICY "Users can update own profile" ON profiles
  FOR UPDATE USING (auth.uid() = id);

-- printers: 본인 프린터만 접근
CREATE POLICY "Users can view own printers" ON printers
  FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Users can insert own printers" ON printers
  FOR INSERT WITH CHECK (auth.uid() = user_id);

-- admin: 관리자 전체 접근
CREATE POLICY "Admins can do anything" ON printers
  FOR ALL USING (
    EXISTS (
      SELECT 1 FROM user_roles
      WHERE user_id = auth.uid()
      AND role IN ('admin', 'superadmin')
    )
  );
```

---

## 부록: 전체 데이터 흐름 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                    FACTOR HIBRID 전체 데이터 흐름                                    │
├─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                                     │
│   ┌─────────────────────────────────────────────────────────────────────────────────────────┐      │
│   │                                    사용자 인터페이스                                      │      │
│   │                                                                                          │      │
│   │    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐       │      │
│   │    │  브라우저 │    │   iOS    │    │ Android  │    │  관리자  │    │   AI     │       │      │
│   │    │   (Web)  │    │  (모바일) │    │ (모바일)  │    │  패널   │    │  사이드바 │       │      │
│   │    └────┬─────┘    └────┬─────┘    └────┬─────┘    └────┬─────┘    └────┬─────┘       │      │
│   └─────────┼───────────────┼───────────────┼───────────────┼───────────────┼─────────────┘      │
│             │               │               │               │               │                     │
│             └───────────────┴───────────────┼───────────────┴───────────────┘                     │
│                                             │                                                     │
│                                             ▼                                                     │
│   ┌─────────────────────────────────────────────────────────────────────────────────────────┐    │
│   │                                    React Application                                     │    │
│   │                                                                                          │    │
│   │    ┌────────────────────────────────────────────────────────────────────────────┐       │    │
│   │    │                           상태 관리 레이어                                   │       │    │
│   │    │                                                                             │       │    │
│   │    │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │       │    │
│   │    │  │React Query  │  │AuthContext  │  │ThemeContext │  │ I18nContext │       │       │    │
│   │    │  │ (서버상태)  │  │  (인증)     │  │  (테마)     │  │   (언어)    │       │       │    │
│   │    │  └──────┬──────┘  └──────┬──────┘  └─────────────┘  └─────────────┘       │       │    │
│   │    └─────────┼────────────────┼─────────────────────────────────────────────────┘       │    │
│   │              │                │                                                          │    │
│   └──────────────┼────────────────┼──────────────────────────────────────────────────────────┘    │
│                  │                │                                                               │
│    ┌─────────────┼────────────────┼─────────────────────────────────────────────────────────┐    │
│    │             │                │                                                          │    │
│    │             ▼                ▼                                                          │    │
│    │  ┌─────────────────────────────────────────────┐                                       │    │
│    │  │              Shared Package                  │                                       │    │
│    │  │                                              │                                       │    │
│    │  │  ┌────────────┐  ┌────────────┐  ┌────────────┐                                    │    │
│    │  │  │  API 클라이언트 │  │  서비스    │  │   훅       │                                    │    │
│    │  │  │  (api/)    │  │(services/) │  │ (hooks/)   │                                    │    │
│    │  │  └─────┬──────┘  └─────┬──────┘  └────────────┘                                    │    │
│    │  │        │               │                                                            │    │
│    │  └────────┼───────────────┼────────────────────────────────────────────────────────────┘    │
│    │           │               │                                                                  │
│    │           │               │                                                                  │
│    │  ┌────────┴───────────────┴────────────────────────────────────────────────────────────┐    │
│    │  │                                백엔드 시스템                                          │    │
│    │  │                                                                                      │    │
│    │  │  ┌────────────────┐   ┌────────────────┐   ┌────────────────┐   ┌─────────────┐   │    │
│    │  │  │                │   │                │   │                │   │             │   │    │
│    │  │  │   Supabase     │   │  MQTT Broker   │   │ Express Server │   │TossPayments │   │    │
│    │  │  │                │   │                │   │                │   │             │   │    │
│    │  │  │ • PostgreSQL   │   │ • Mosquitto    │   │ • REST API     │   │ • 결제 처리  │   │    │
│    │  │  │ • Auth         │   │ • WebSocket    │   │ • WebSocket    │   │ • 구독 관리  │   │    │
│    │  │  │ • Storage      │   │   브릿지       │   │   브로드캐스트  │   │             │   │    │
│    │  │  │ • Realtime     │   │                │   │                │   │             │   │    │
│    │  │  │                │   │                │   │                │   │             │   │    │
│    │  │  └───────┬────────┘   └───────┬────────┘   └───────┬────────┘   └──────┬──────┘   │    │
│    │  │          │                    │                    │                   │          │    │
│    │  └──────────┼────────────────────┼────────────────────┼───────────────────┼──────────┘    │
│    │             │                    │                    │                   │               │
│    │             │                    │                    │                   │               │
│    │  ┌──────────┼────────────────────┼────────────────────┼───────────────────┼──────────┐    │
│    │  │          │                    │                    │                   │          │    │
│    │  │          ▼                    ▼                    ▼                   ▼          │    │
│    │  │  ┌────────────────────────────────────────────────────────────────────────────┐  │    │
│    │  │  │                          외부 시스템                                        │  │    │
│    │  │  │                                                                             │  │    │
│    │  │  │  ┌─────────────┐   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐    │  │    │
│    │  │  │  │  3D 프린터   │   │   카메라    │   │  AI 서버    │   │ OAuth 제공자│    │  │    │
│    │  │  │  │ (OctoPrint) │   │ (MJPEG)     │   │(Image-to-3D)│   │(Google/Apple)│   │  │    │
│    │  │  │  └─────────────┘   └─────────────┘   └─────────────┘   └─────────────┘    │  │    │
│    │  │  │                                                                             │  │    │
│    │  │  └─────────────────────────────────────────────────────────────────────────────┘  │    │
│    │  │                                                                                    │    │
│    │  └────────────────────────────────────────────────────────────────────────────────────┘    │
│    │                                                                                             │
│    └─────────────────────────────────────────────────────────────────────────────────────────────┘
│                                                                                                   │
└───────────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 문서 정보

| 항목 | 내용 |
|------|------|
| **문서명** | FACTOR HIBRID 웹 아키텍처 및 데이터 흐름 |
| **버전** | 1.0 |
| **작성일** | 2025-11-25 |
| **대상** | 웹 플랫폼 (packages/web) |
| **기술 스택** | React 18, TypeScript, Vite, TanStack Query, Supabase, MQTT, TossPayments |

---

*이 문서는 FACTOR HIBRID 프로젝트의 웹 아키텍처를 이해하기 위한 참조 문서입니다.*
